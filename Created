3) NESTED IF THEN ELSE

  DECLARE 
  HOUR VARCHAR(10):= TO_CHAR(SYSDATE,'HH24');
  BEGIN
  IF HOUR > 16 THEN
  DBMS_OUTPUT.PUT_LINE('GOOD MORNING');
  ELSEIF HOUR > 12 THEN
  DBMS_OUTPUT.PUT_LINE('GOOD AFTERNOON');
  ELSE
  DBME_OUTPUT.PUT_LINE('GOOD NIGHT');
  END IF;
  END;
  /
